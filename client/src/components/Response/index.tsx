const Response = ({ text }: { text: string }) => {
  return (
    <div className="w-[39.31rem] h-auto flex justify-center items-center p-3 rounded-[16px] bg-[#CCCCCC]/25">
      {text}
    </div>
  );
};

export default Response;
